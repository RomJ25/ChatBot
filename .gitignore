node_modules
dist
.DS_Store
*.log
.vite
.env.local
.env.*.local
*.tsbuildinfo
.playwright-mcp

# .pnpm-store/ is INTENTIONALLY tracked — see .npmrc. It vendors pnpm's
# content-addressable tarball cache so `pnpm install --offline` works on
# air-gapped machines after a fresh clone.

# Local dev screenshots
/*.png

# Standalone-binary launcher artifacts (scripts/bundle.ts)
/out/
/scripts/launcher/embedded.sniro.ts
/scripts/launcher/embedded.de-vincho.ts
/scripts/launcher/embedded.active.ts
/scripts/launcher/embedded.*.assets/
# Launcher env files created next to binaries during local testing
*.env

# Scan-safe deploy bundles produced by scripts/safe-deploy.mjs.
/deploy-sniro/
/deploy-de-vincho/
